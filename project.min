# 杭州线下当天比赛活动页需求

## 目标

为杭州线下当天比赛准备一个本地活动页，由主办方单人操作后台控制区，并通过电脑投屏展示给选手和观众。

页面重点是现场稳定、流程清楚、避免误操作。不要做登录系统，不做复杂权限，不做当天实时内容生成。

## 页面角色

### 后台控制区

仅主办方本人使用。

需要支持：

- 查看报名选手列表。
- 选中当前选手。
- 为当前选手点击抽取主题。
- 抽题后同时显示该主题以及对应 PPT 信息。
- 锁定抽题结果，且不允许重新抽取。
- 查看抽题记录。
- 管理或关联统一收集好的 PPT 文件。

### 投屏展示页

给现场选手和观众观看。

需要展示：

- 当前选手的简短简介。
- 当前抽到的主题。
- 当前主题对应的 PPT 信息或文件入口。
- Lightning Talk / Demo 环节需要展示的 Demo 信息。

## 选手简介

选手信息来自报名表，比赛前清洗后导入。

展示字段保持简短，只包含：

- 昵称。
- 线上 / 线下。
- Coding 阶段，或选手自己提供的三个描述词。

报名表中的手机号、微信、邮箱等隐私信息不进入活动页展示数据。

## 抽题与 PPT

抽题和 PPT 内容都在比赛前准备好。

现场点击抽题时，只是在已准备好的主题 / PPT 池中随机选中并展示，不调用 AI，不实时生成 PPT。

抽题规则：

- 每位选手只能抽取一次。
- 抽取后立即记录选手、主题、PPT、时间。
- 抽取结果锁定后不允许重新抽取。
- 控制区需要能清楚看到哪些选手已经抽过题。

PPT 管理：

- PPT 由主办方在比赛前统一收集和上传 / 关联。
- 浏览器不需要直接播放 PPTX。
- 现场可以通过序号、昵称、主题或文件入口对应到具体 PPT。
- 如需投屏，优先使用已准备好的文件入口或下载后本地打开。

## Demo 展示

Demo 展示属于 Lightning Talk 可能使用的辅助环节，优先级低于抽题和 PPT 流程。

每个 Demo 可以包含：

- Demo 名称。
- 简短说明。
- 链接或本地素材入口。

不要求做复杂的 Demo 管理系统。

## 数据准备

比赛前从报名表导出数据并清洗为活动页可直接读取的数据文件。

数据文件至少包含：

- 选手基础展示信息。
- 线上 / 线下标记。
- Coding 阶段或三个描述词。
- 可选 Demo 信息。
- PPT 对应信息。

## 不做

- 不做选手登录。
- 不做观众操作入口。
- 不做当天实时生成 PPT。
- 不做在线协作后台。
- 不做复杂权限系统。
- 不做重新抽题机制。
